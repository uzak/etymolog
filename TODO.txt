* introduce tags?
* use cases:
    - lookup a `word` (for a `language`)
    - give me a dictionary with derivations (in * or given languages) for a given language
# COMMENT => LBRACE, RBRACE (and parse just like anything)?

